#pragma once

class GLRender
{
public:
    bool Initgl(GLFWwindow* hwnd);
};

// delete arit folder.
//why?
// we're using the "maths" folder for the maths stuff.
//arit is aritmetics which is what maths is part of